const fs = require('fs');
let code = fs.readFileSync('src/components/Sidebar.tsx', 'utf8');
// Fix the icon for Painel do Profissional (dashboard) to match the other icons properly, maybe sync_alt was nice.
// Actually what I did is fine.
